# 设计稿 ↔ 规范文档 映射与同步

> **本文件只做映射与流程，不含任何取值。** 取值唯一来源始终是 `foundation/tokens.md`。

---

## 1. 为什么要做这件事

规范的数值来自设计稿，但「设计稿 → 规范文档」这一段长期靠**人工转录**。
转录就会出错，而且出错后极难发现——因为设计稿和规范里的两个数都「看起来合理」。

已经反复出现的误差类型：

| 误差 | 表现 | 发现方式 |
| :-- | :-- | :-- |
| 两个相近尺寸被记成同一个数 | 例如弹窗头高与抽屉头高本是两个值 | 规范内部两处文档打架时才暴露 |
| 同值不同名被当成可互换 | 两个 Token 当前取值相同，于是互相代用，语义被绑死 | 语义统一时才发现 |
| 缺档自行插值 | 设计稿色阶没导全，凭感觉补中间色 | 对照原始导出才对不上 |
| 透明度被丢掉 | 设计稿「黑色 60%」抄成不透明纯色 | 渲染后遮罩不对才发现 |

共同点：**都不是写代码写错，是抄的时候抄错或抄漏。**
所以解法不是「下次仔细点」，而是让两边能被机器比对。

---

## 2. 映射表：规范分组 ↔ 设计稿侧

规范侧以 `tokens.md` 的章节分组为准。下表是**分组级**映射——
设计稿变量名不必与 Token 同名（同名更好，可直连），但必须能归到同一分组。

| 规范分组（tokens.md 章节） | 设计稿侧对应 | 同步优先级 |
| :-- | :-- | :-- |
| 颜色变量 | 品牌主色 / hover / active / 浅底 / focus | **高** |
| 功能色 | 成功 / 警告 / 错误 / 信息四组 | **高** |
| 文本色 | 文字色阶（标题 / 正文 / 辅助 / 禁用 / 反色） | **高** |
| 边框与填充 | 分割线 / 输入框边框 / 禁用填充 | **高** |
| 布局与组件尺寸 | 顶栏高 / 侧边栏宽 / 浮层各档宽高 | **高** |
| 表格 | 表头底 / 斑马纹 / 行 hover | 中 |
| 导航 | 顶栏 / 侧边栏 / 菜单各级底色与文字色 | 中 |
| 卡片 / 标签 / 图表系列色 | 对应组件色板 | 中 |
| 涨跌色 | 涨 / 跌两色 | **高**（金融类中后台） |
| 阴影 | 阴影层级导出 | 中 |
| 圆角 / 间距 / 字体 / 过渡 / z-index | 对应档位 | 低 |

> 设计稿若使用 Figma 原生变量（Variables）或 Tokens Studio，
> 建议变量名与 `--` 前缀 Token **同名**（去掉 `--` 即可），核对脚本能直接一一对应。

---

## 3. 同步流程

```
设计稿变更（新增/改值/删变量）
   ↓
设计师导出变量文件（JSON / CSV，见 scripts/design-export.sample.*）
   ↓
node <package>/scripts/check-design-sync.js <导出文件>
   ↓
三类差异 → 按下表决策 → 改规范 or 改设计稿
   ↓
规范侧落定后：先改 tokens.md → 再回灌模板 :root → 跑 check-tokens.js --strict
```

### 差异决策表

| 差异 | 含义 | 怎么决策 |
| :-- | :-- | :-- |
| **D3 同名不同值** | 设计稿与规范取值不一致（**转录误差，最该关注**） | 先确认哪边是「意图源」：设计稿是新基线 → 改 `tokens.md` 再回灌；规范先调整 → 回写给设计稿。**不允许两边各留一个数** |
| **D1 设计稿有、规范无** | 规范没收编 | 用得上就收编；确属色板预留 → 用途列标注「预留」并指向替代 Token |
| **D2 规范有、设计稿无** | 设计稿未同步，或规范虚胖 | 设计稿确实需要 → 补到设计稿；规范侧已不用 → 标注预留或删除 |
| **名字对不上** | 命名不一致，无法配对 | 优先改设计稿变量名与 Token 同名；不能改时，在导出文件里用别名列显式指定 |

### 铁律

1. **先改规范，再回灌模板** —— 反向操作必然产生双源漂移
2. **改完必跑** `check-tokens.js . --strict`，退出码必须是 0
3. **新 Token 不得凭空产生**

---

## 4. 转录误差高发点（核对时重点看）

1. **相近但不同的尺寸**：浮层头高、按钮各档高度这类「看起来差不多」的值，禁止笼统记成一个
2. **透明度换算**：设计稿给「黑色 60%」，规范里应是 `rgba(...)`，抄成不透明纯色是最常见错误
3. **同值不同名的 Token**：当前取值相同 ≠ 可以互换，语义会各自演进
4. **色阶插值**：缺档位**禁止自行插值**，必须从设计稿原始导出查
5. **格式差异**：大小写、空格、单位（`0.6` / `60%`）——核对脚本会归一化，但规范里请统一写法
6. **组件级别名 Token**：改基础 Token 时容易漏改别名

---

## 5. 核对脚本

```bash
node <package>/scripts/check-design-sync.js <设计稿导出文件> [--strict] [--json] [--quiet]
```

- 自动识别四种导出形态：Figma 原生变量、Tokens Studio JSON、扁平 JSON、CSV/TSV
- 输出 D1 / D2 / D3 三类差异；预留标记单列，不计入 D2
- 退出码：0 = 无差异；1 = 存在差异（`--strict` 时 D2 也计失败）
- 只读取、只报告，**不改任何文件**

---

## 6. 维护

- 设计稿升级大版本：更新 §2 映射表 → 重新导出 → 跑核对 → 处理差异 → 在 `tokens.md` 变更记录留痕
- 新增规范分组：在 §2 补一行，否则该组变量不进入核对范围
- 发现新的转录误差类型：补进 §4，让下一个人少踩一次

---

初始化时间：{{DATE}}　系统：{{SYSTEM}}
