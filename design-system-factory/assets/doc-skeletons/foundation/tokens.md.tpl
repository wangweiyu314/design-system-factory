# {{SYSTEM}} 设计 Token 汇总

> **本文件是取值的唯一来源。** 任何颜色、字号、间距、圆角、阴影、层级取值都必须在这里登记后被引用。
> 在别处抄一份数值，就是给下一次取值漂移埋雷。

---

## 填写规则（动手前必读）

1. **表格格式是硬要求**，校验器按下面的格式解析本文件来比对交付物：

```
| Token | 默认值 | 用途 |
|-------|--------|------|
| --color-<语义> | #RRGGBB | 用途说明 |
```

   - 第一列必须是 `--` 开头的 Token 名
   - 第二列是取值；**留空或写 `--` 的占位行不会被解析**，可以放心写待填骨架
   - 第三列是用途；含「预留 / 色板 / 暂不使用 / 不适用 / 保留 / 废弃」的行会被识别为预留，不计入「规范未落地」

   > **⚠ 示例行会被校验器当成真 Token 解析。** 上面用了 `--color-<语义>` 这种带尖括号的占位名，
   > 就是为了让解析器匹配不到——写成 `--color-primary | #2A6CDD` 的话，
   > 这个示例值会被登记成规范取值，等你把真主色填进模板，就会报「取值漂移」。
   > 文档里凡是举例，都用这种匹配不到的占位名。

2. **先改本文件，再回灌模板 `:root`**。反向操作必然产生双源漂移。

3. **新 Token 不得凭空产生**：要么来自设计稿（收编），要么来自实现需要（登记后再回灌）。

4. **语义命名不可由机器裁决**。挖掘脚本给的是频次排序，主色/成功/警告/错误必须有设计师确认。

---

## 颜色变量

<!-- 按用途分组：品牌主色 → 功能色 → 文本色 → 边框与填充 → 背景 → 语义色 -->

| Token | 默认值 | 用途 |
|-------|--------|------|
|  |  |  |

### 待确认清单

- [ ] 主色及其 hover / active / 浅底 / focus ring
- [ ] 功能色四组：成功 / 警告 / 错误 / 信息（文本色 + 背景色）
- [ ] 文本色阶：标题 / 正文 / 辅助 / 禁用 / 反色
- [ ] 边框与分割线：一级 / 二级
- [ ] 涨跌色（金融类中后台必填，境内惯例为涨红跌绿）
- [ ] 图表系列色

---

## 间距变量

| Token | 默认值 | 用途 |
|-------|--------|------|
|  |  |  |

> 网格基数：______px（由 `mine-tokens.js` 拟合，需人工确认）

---

## 圆角变量

| Token | 默认值 | 用途 |
|-------|--------|------|
|  |  |  |

---

## 阴影变量

| Token | 默认值 | 用途 |
|-------|--------|------|
|  |  |  |

---

## 字体变量

| Token | 默认值 | 用途 |
|-------|--------|------|
|  |  |  |

---

## 布局与组件尺寸 Token

| Token | 默认值 | 用途 |
|-------|--------|------|
|  |  |  |

---

## 过渡与层级 Token

| Token | 默认值 | 用途 |
|-------|--------|------|
|  |  |  |

---

## 变更记录

| 日期 | 变更 | 来源 |
| :-- | :-- | :-- |
| {{DATE}} | 建包，取值待填 | {{SYSTEM}} 设计系统初始化 |
